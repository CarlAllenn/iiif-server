//! The server binary. hyper 1.x directly on tokio — no web framework; the
//! IIIF grammar in `iiif-core` *is* the router.

fn main() {
    println!("iiif-server: M0 skeleton — serving arrives with the HTTP layer");
}
